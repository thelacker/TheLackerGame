#include <windows.h>

void StartGame(){
	int X = GetSystemMetrics(SM_CXSCREEN);
	int Y = GetSystemMetrics(SM_CYSCREEN);
	//Exit.Create(_T("Exit"), WS_CHILD | WS_VISIBLE | BS_PUSHBUTTON, CRect(10, 170 + 40, rect.Width() - 30, 220 + 40), this, IDC_exb);
};